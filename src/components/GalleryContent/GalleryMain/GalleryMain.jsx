import GalleryContent from "../GalleryContent";

const GalleryMain = (props) => {
    return (
        <main>
            <GalleryContent random="randomprops" />
        </main>
    )
}

export default GalleryMain;